# 智分Design V3.1 产品规格书（最终版）

> 综合天越TYCAD、天越AI版、infoCAD、AIDP、Ekahau AI Pro等主流室分设计软件优点，集大成之作。

---

## 一、产品定位

**专业室内分布系统设计CAD软件**，独立运行（不依赖AutoCAD/Visio），面向通信设计院、运营商、集成商的室分工程师。

**核心差异化**：
1. 真正的独立CAD内核（非插件），统一模型空间
2. AI自动布防引擎（多目标优化，比人工设计省8.9%~20.1%成本）
3. 实时链路预算+全自动功率标注
4. 平面图与系统图联动，逆向功率优化

---

## 二、对标软件优点吸收

| 软件 | 吸收的优点 |
|------|-----------|
| **天越TYCAD** | CAD原生操作、7专用工具栏、平面图转系统图4种模式、自动编号ANTn-mF、主干优化逆推算法、智能打印、电梯覆盖一键生成、标签系统(可加运营商LOGO)、图纸对比、系统图切割 |
| **天越AI版** | 云架构AI功率优化、一键批量出图、多制式仿真(2G/3G/4G/5G/WLAN)、数字化室分支持(华为lampsite/中兴/爱立信Radiodot/诺基亚FlexiZone) |
| **infoCAD** | 精准功率计算、多频段支持(700M/800M/900M/1800M/2100M/2600M/3.5G)、面向铁塔共建共享 |
| **AIDP** | CAD底图导入、环境数据自动识别(墙体/门窗/天花板)、自动化DAS设计 |
| **Ekahau AI Pro** | AI Auto-Planner数万次迭代找最优点位、自动墙体检测、自动区域检测、多楼层联合优化、辅助规划高亮推荐位置 |
| **浩辰AI** | 自然语言驱动操作、AI图片转CAD矢量、批量智能编辑 |

---

## 三、系统架构

### 3.1 统一模型空间（核心）

**所有内容在同一个CAD模型空间中**：
- 各楼层平面图按origin坐标横向排列（间距5000mm）
- 系统图画在平面图右侧（非比例线，只表连接关系）
- 楼层之间用虚线分界+楼层标签
- 用户缩放平移查看任意区域，框选打印任意区域

**不再有"楼层切换"概念**，只有"楼层定位"（双击楼层标签跳转到对应区域）。

### 3.2 图层系统

| 图层名 | 内容 | 颜色 | 默认 |
|--------|------|------|------|
| 底图 | 建筑CAD底图/图片 | 灰色8 | 锁定+半透明 |
| 墙体 | 建筑墙体 | 白色 | 开 |
| 器件 | 天线/功分器/耦合器/合路器/信源 | 各制式颜色 | 开 |
| 馈线 | 1/2"/7/8"/跳线 | 青色 | 开 |
| 标注 | 功率/长度/编号/标签 | 绿色 | 开 |
| 系统图 | 系统图器件+馈线 | 各制式颜色 | 开 |
| 热力图 | 信号覆盖仿真 | 半透明彩色 | 关 |
| 辅助 | 网格/捕捉点/楼层分界 | 红色虚线 | 关 |

每个图层支持：开关显示、锁定、冻结、颜色修改。

### 3.3 模块架构

```
智分Design
├── CAD内核层
│   ├── 模型空间（统一坐标）
│   ├── 视图引擎（缩放/平移/框选）
│   ├── 图层管理
│   ├── 捕捉系统（端点/中点/交点/垂足/最近点）
│   └── 命令行+状态栏
├── 专业功能层
│   ├── 器件库（6大类40+型号）
│   ├── 平面图设计（馈线绘制+自动插器件）
│   ├── 系统图生成（4种模式+逆向优化）
│   ├── 链路预算（实时+5位置全标）
│   ├── 自动编号（ANTn-mF规范）
│   ├── 主干优化（多楼层逆推汇聚）
│   ├── 材料统计（CSV/Excel）
│   ├── 造价概算（40+器件价格表）
│   └── 电梯覆盖一键生成
├── AI智能层
│   ├── 快速布防（贪心网格，秒级）
│   ├── 智能优化（GA多目标，分钟级）
│   ├── 覆盖仿真（射线追踪+热力图）
│   └── 盲区检测+补点建议
└── 交付层
    ├── 框选打印（窗口/范围/全部）
    ├── 批量打印（多区域连续出图）
    ├── 图片导出（PNG/JPG/PDF）
    ├── DXF导出
    ├── 设计报告（HTML）
    └── 标签打印
```

---

## 四、UI界面规范

### 4.1 经典CAD布局

```
┌─────────────────────────────────────────────────────┐
│  标题栏：智分Design V3.1 - [工程名.zfp]            │
├─────────────────────────────────────────────────────┤
│  菜单栏：文件 编辑 视图 插入 格式 工具 绘图 标注 帮助 │
├─────────────────────────────────────────────────────┤
│  标准工具栏：新建 打开 保存 打印 撤销 重做 复制 粘贴  │
├─────────────────────────────────────────────────────┤
│  室分工具栏：平面图|系统图|优化|排版|基本工具|图层|仿真 │
├──────┬──────────────────────────────────────┬───────┤
│      │                                      │       │
│ 器件 │                                      │ 属性  │
│ 列表 │         绘图区（模型空间）            │ 面板  │
│      │                                      │       │
│      │                                      │       │
├──────┴──────────────────────────────────────┴───────┤
│  命令行：命令: _                                     │
├─────────────────────────────────────────────────────┤
│  状态栏：X=1234.5 Y=678.9  捕捉 正交 极轴 1:100 缩放 │
└─────────────────────────────────────────────────────┘
```

### 4.2 7个室分专用工具栏

**1. 平面图工具栏**
- P连接点（信源）、天线、功分器、耦合器(4方向)、合路器、负载、衰减器、干放
- 馈线（1/2"/7/8"/跳线）、自动插器件、自动打断标长度
- 电梯覆盖一键生成

**2. 系统图工具栏**
- 生成系统图（4种模式选择）、逆向功率优化、楼层分界设置
- 系统图切割、图框压缩

**3. 优化工具栏**
- 功率自动优化、主干优化（多楼层汇聚）、器件选型推荐
- 功率越限检查、损耗告警

**4. 排版工具栏**
- 自动排版、标签输出、图框插入、图签填写
- 图纸对比

**5. 基本工具工具栏**
- 删除、移动、复制、旋转、缩放、拉伸、延伸、修剪
- 轻量复制（天越特色：复制时自动重编号）

**6. 图层工具栏**
- 图层开关、锁定、冻结、颜色、当前图层设置
- 图层管理器

**7. 仿真/电平工具栏**
- 电平预测、覆盖仿真、热力图、盲区检测
- AI自动布防

### 4.3 状态栏

- 左侧：光标实时坐标 `X=1234.50 Y=678.90`
- 中间：捕捉开关（端点/中点/交点/垂足/最近点）、正交、极轴、对象追踪
- 右侧：当前比例 `1:100`、缩放比例、当前图层名

### 4.4 命令行

支持CAD命令：`LINE`、`COPY`、`MOVE`、`ERASE`、`ZOOM`、`PAN`、`PRINT`等。
室分专用命令：`FEEDER`（画馈线）、`AUTOINSERT`（自动插器件）、`GENDIAGRAM`（生成系统图）、`AIPLACE`（AI布防）。

---

## 五、功能模块详细规格

### 5.1 器件库

**6大类40+型号**：

| 类别 | 型号 | 关键参数 |
|------|------|---------|
| **信源** | 宏基站耦合、微基站、直放站、干放、数字信源(华为/中兴/爱立信/诺基亚) | 输出功率(dBm)、端口数、支持频段 |
| **天线** | 全向吸顶、定向壁挂、对数周期、电梯天线、美化天线 | 增益(dBi)、水平/垂直波瓣角、功率容量 |
| **功分器** | 二功分、三功分、四功分 | 分配损耗+插损、功率容量、频段 |
| **耦合器** | 5/6/7/10/15/20/25/30dB | 耦合度+直通插损、功率容量 |
| **合路器** | 双频合路、三频合路、多频合路 | 插损、隔离度、频段 |
| **其他** | 负载、衰减器(5/10/15/20dB)、避雷器、跳线 | 衰减值、功率容量 |

**器件图元**：每个器件有标准CAD图块（比例固定），含器件编号标签位置。
**器件参数**：馈线衰减(dB/100m)、接头损耗(0.05dB/个)、器件插损均内置标准值，可在系统配置中修改。

### 5.2 平面图设计

**操作流程**：
1. 导入建筑底图（DXF/DWG/图片），设置比例
2. 放置P连接点（信源），设置输出功率
3. 放置天线（手动或AI布防）
4. 画馈线连接（从信源→功分器/耦合器→天线）
5. 馈线交点处自动插入器件（或手动选择插入）
6. 馈线自动打断并标注长度
7. 实时显示链路功率

**馈线规则**：
- <5米：跳线（黄色）
- 5~50米：1/2"馈线（青色）
- >50米：7/8"馈线（蓝色）
- 可手动修改馈线型号

**自动插器件**：
- 画好馈线后，点击"自动插器件"按钮
- 鼠标移到馈线交点处，预览器件类型
- 左击确认，自动插入并调整馈线连接
- 耦合器支持4方向（上/下/左/右）

### 5.3 系统图生成

**4种走线模式**：
- 模式A：功分器下方输出口馈线水平，其余向上
- 模式B：功分器各输出口馈线对称
- 模式C：功分器输出口直接向上/向下对称
- 模式D：功分器输出口馈线水平

**生成规则**：
- 系统图为非比例线，只表连接关系
- 楼层之间用虚线分界，标注楼层号
- 器件位置自动排版，避免重叠
- 生成后可手动调整位置

**逆向功率优化**：
- 从天线目标功率逆推，自动调整耦合度/功分器选型
- 目标：所有天线口功率偏差最小（<2dB）
- 输出优化建议（如"12dB耦合器→10dB，补偿1.5dB"）

### 5.4 链路预算与标注

**5个位置强制标注功率**：
1. 信源输出口
2. 合路器输出口
3. 耦合器直通端+耦合端
4. 功分器各输出口
5. 天线口

**馈线标注**：`-x.xdB / xx.xm`（线损/长度）
**器件标注**：输入功率→输出功率（dBm）
**天线标注**：`xx.xdBm / ANTn-mF`（功率/编号）

**实时计算**：修改任何器件或馈线，功率标注自动更新。
**越限告警**：天线口功率>15dBm或<目标值时标红。

### 5.5 自动编号

**编号规范**：
- 天线：`ANTn-mF`（n=每层序号，m=楼层号）
- 功分器：`PSn-mF`
- 耦合器：`Tn-mF/xdB`（x=耦合度）
- 合路器：`CBn-mF`
- 负载：`LDn-mF`
- 衰减器：`ATn-mF`
- 干放：`RPn-mF`

**编号规则**：每层从1开始，按信号流向排序。
**轻量复制**：复制器件时自动重新编号，避免重复。

### 5.6 主干优化

**逆推算法**：
1. 从所有天线开始，按馈线损耗值排序
2. 逐级向前汇聚，自动选择功分器/耦合器
3. 多楼层系统图汇聚到同一主干
4. 输出统一的整体方案

**效果**：减少主干馈线长度，降低成本，统一功率分配。

### 5.7 材料统计与造价

**材料表**：
- 按器件类型分类统计数量
- 馈线按型号统计长度（1/2"/7/8"/跳线分别统计）
- 接头数量自动计算
- 输出CSV/Excel格式

**造价概算**：
- 内置40+器件单价表
- 馈线按米计价
- 安装费按比例计取
- 输出总造价+分项造价

### 5.8 电梯覆盖

**一键生成**：
- 选择电梯井位置+楼层范围
- 自动生成电梯天线方案（对数周期天线或电梯专用天线）
- 自动计算馈线路由和长度
- 自动加入系统图

### 5.9 出图与打印

**图框**：
- A1（平面图）、A2（系统图）标准图框
- 右下角标题栏：工程名称、项目编号、设计阶段、图纸比例、日期、设计/审核/批准
- 支持运营商标准图框模板

**打印方式**：
- 窗口打印：框选任意区域打印
- 范围打印：按楼层/系统图区域自动识别
- 全部打印：整个模型空间
- 批量打印：多个窗口区域连续出图

**导出格式**：PNG、JPG、PDF、DXF

### 5.10 工程管理

- 工程保存为`.zfp`格式（JSON，含所有数据）
- 最近打开项目列表
- 工程模板（写字楼/酒店/商场/地下车库4种预设）
- 工程信息：站点名称、地址、经纬度、楼层数、建筑面积、运营商

---

## 六、AI自动布防引擎（核心特色）

### 6.1 数学模型

**多目标优化问题**：
- 目标1：最小化部署成本（器件费+线缆费）
- 目标2：最小化天线间最大输出功率偏差（功率均衡）
- 约束：每个天线口功率≥目标值（如≥10dBm），覆盖≥-85dBm

### 6.2 三层架构

**第一层：快速布防（贪心启发式）**
- 根据楼层边界+覆盖半径（6~8米）网格布放天线
- 自动放置信源+功分器+耦合器
- 秒级出方案，用于快速预览

**第二层：智能优化（遗传算法GA）**
- 以第一层方案为初始解
- 哈夫曼树编码表示室分拓扑结构
- 优化变量：天线位置、器件选型、拓扑结构
- 适应度函数：成本×0.5 + 功率偏差×0.5
- 种群大小100，迭代200代
- 输出Pareto前沿3个方案：低成本/高均衡/折中
- 分钟级计算

**第三层：覆盖验证（射线追踪）**
- 对优化方案进行信号覆盖仿真
- 墙体穿透损耗自动计算
- 输出热力图，标注盲区
- 盲区自动补点建议

### 6.3 关键参数

| 参数 | 默认值 | 范围 |
|------|--------|------|
| 天线覆盖半径 | 7米 | 5~12米 |
| 目标天线口功率 | 10dBm | 5~15dBm |
| 覆盖电平要求 | -85dBm | -75~-95dBm |
| 墙体衰减(240mm) | 15dB | 10~25dB |
| 玻璃衰减 | 6dB | 3~10dB |
| 迭代次数 | 200代 | 50~500 |
| 种群大小 | 100 | 50~300 |

### 6.4 操作流程

1. 导入建筑底图，设置楼层边界
2. 点击"AI自动布防"
3. 设置参数（覆盖半径、目标功率、运营商制式）
4. 选择模式：快速/智能优化
5. 等待计算（快速秒级/智能1~3分钟）
6. 展示3个方案对比（成本/功率偏差/覆盖率）
7. 选择方案应用到图纸
8. 自动生成系统图+功率标注+材料表

---

## 七、制图规范参数（内置标准）

### 7.1 馈线衰减

| 馈线型号 | 900MHz | 1800MHz | 2100MHz | 2600MHz | 3.5GHz |
|----------|--------|---------|---------|---------|--------|
| 1/2" | 6.87 dB/100m | 9.85 | 10.7 | 12.0 | 14.5 |
| 7/8" | 3.87 dB/100m | 5.54 | 6.03 | 6.76 | 8.2 |
| 跳线(1/4") | 14.0 dB/100m | 20.0 | 21.8 | 24.4 | 29.5 |
| 接头 | 0.05 dB/个 | 0.05 | 0.05 | 0.05 | 0.05 |

### 7.2 器件损耗

| 器件 | 损耗 |
|------|------|
| 二功分器 | 3.5 dB（含插损） |
| 三功分器 | 5.3 dB（含插损） |
| 四功分器 | 6.5 dB（含插损） |
| 6dB耦合器 | 耦合6.0 / 直通1.5 |
| 10dB耦合器 | 耦合10.0 / 直通0.6 |
| 15dB耦合器 | 耦合15.0 / 直通0.4 |
| 20dB耦合器 | 耦合20.0 / 直通0.3 |
| 合路器 | 0.5~1.0 dB |
| 负载 | - |

### 7.3 出图规范

- 图幅：A1平面图、A2系统图
- 比例：1:100（常规）、1:200（大面积）
- 线宽：轮廓0.3mm，其他0.1mm
- 图名下画粗实线，比例字号比图名小一号
- 标题栏右下角，粗实线图框

---

## 八、技术实现方案

### 8.1 技术栈

- **语言**：C++17
- **构建**：CMake 3.16+
- **GUI**：Qt 5.15.2（Widgets + PrintSupport）
- **JSON**：nlohmann/json（单头文件）
- **CAD绘制**：QPainter（模型坐标→视图坐标变换）
- **云端构建**：GitHub Actions（windows-latest + MSVC + Inno Setup）

### 8.2 核心数据结构

```
Project
├── projectInfo（工程信息）
├── systemConfig（制式/颜色/线损配置）
├── layers[]（图层列表）
├── floors[]（楼层列表，每个有origin坐标）
│   ├── walls[]（墙体）
│   ├── devices[]（器件实例，含position）
│   └── cables[]（馈线段，含start/end/length）
├── systemDiagram（系统图数据，origin在平面图右侧）
├── materials[]（材料统计）
└── aiConfig（AI布防参数）
```

### 8.3 坐标系统

- 模型坐标：毫米(mm)，原点在左下角
- 楼层排列：Floor[0].origin=(0,0)，Floor[1].origin=(楼层宽度+5000, 0)，依次右排
- 系统图origin=(所有楼层总宽度+10000, 0)
- 视图变换：QTransform实现缩放平移，鼠标滚轮缩放，中键/右键拖拽平移

---

## 九、开发路线图

### 阶段一：CAD骨架重构（R1）
- [ ] 统一模型空间（所有楼层+系统图同画布）
- [ ] 图层管理系统
- [ ] CAD状态栏（坐标+捕捉+正交+比例）
- [ ] 命令行基础
- [ ] 窗口框选打印+批量打印
- [ ] 楼层定位（双击标签跳转）

### 阶段二：专业功能完善（R2）
- [ ] 馈线绘制+自动打断标长度
- [ ] 自动插器件（交点识别+4方向耦合器）
- [ ] 实时链路预算+5位置功率标注
- [ ] 自动编号（ANTn-mF规范）
- [ ] 平面图转系统图（4种模式）
- [ ] 逆向功率优化

### 阶段三：高级功能（R3）
- [ ] 主干优化（多楼层逆推汇聚）
- [ ] 材料统计+造价概算
- [ ] 电梯覆盖一键生成
- [ ] 标准图框+标题栏
- [ ] DXF导出
- [ ] 设计报告HTML

### 阶段四：AI自动布防（R4）
- [ ] 快速布防（贪心网格）
- [ ] 智能优化（GA多目标+哈夫曼编码）
- [ ] 覆盖仿真（射线追踪+热力图）
- [ ] 盲区检测+补点建议
- [ ] 多方案对比选择

### 阶段五：打磨交付（R5）
- [ ] UI细节优化
- [ ] 性能优化（大数据量流畅度）
- [ ] 错误处理+用户提示
- [ ] 安装包美化
- [ ] 用户手册

---

## 十、验收标准

1. **CAD本质**：所有楼层平面图+系统图在同一画布，可缩放平移，框选打印任意区域
2. **专业规范**：器件编号ANTn-mF、馈线标-xdB/xm、5位置功率全标、越限标红
3. **AI布防**：输入楼层边界→自动出方案→成本比人工低≥5%→功率偏差≤3dB
4. **出图交付**：A1/A2图框、标题栏、批量打印、PNG/PDF/DXF导出
5. **性能**：100个器件+200段馈线场景下，缩放平移流畅（≥30fps），功率计算实时（<100ms）
